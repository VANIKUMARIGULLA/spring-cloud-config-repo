server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: http://github.com/VANIKUMARIGULLA/spring-cloud-config-repo
          default-label: main
          clone-on-start: true
          skip-ssl-validation: true
